# 开箱即用的国标28181和部标808+1078协议视频平台

WEB VIDEO PLATFORM是一个基于GB28181-2016标准实现的网络视频平台，负责实现核心信令与设备管理后台部分，支持NAT穿透，支持海康、大华、宇视等品牌的IPC、NVR、DVR接入。支持国标级联，支持rtsp/rtmp等视频流转发到国标平台，支持rtsp/rtmp等推流转发到国标平台。

https://doc.wvp-pro.cn/#/

https://gitee.com/pan648540858/wvp-GB28181-pro

开箱即用的28181协议视频平台。



```
 html
 <div class="ttt" @pointerdown="handleDown(1)" @pointerup="handleUp(1)"></div>
 <div class="lll" @pointerdown="handleDown(2)" @pointerup="handleUp(2)"></div>
 <div class="rrr" @pointerdown="handleDown(3)" @pointerup="handleUp(3)"></div>
 <div class="bbb" @pointerdown="handleDown(4)" @pointerup="handleUp(4)"></div>
 
 
 
 const handleDown = (type) => {
  let command = ['', 'up', 'left', 'right', 'down']
  let d = currentData.value
  if (d) {
    console.log('dddd', d);
    // ptz控制command：['left','right','up','down','stop'] horizonSpeed:水平速度0-255  verticalSpeed:垂直速度0-255
    fetch(`http://39.150.**.**:81/api/front-end/ptz/${d.wvpDeviceId}/${d.wvpChannelId}?command=${command[type]}&horizonSpeed=80&verticalSpeed=80&zoomSpeed=4`,
      {
        method: "GET",
        headers: {
          "access-token": `${myAccessToken}`,
          "Content-Type": "application/json",
        },
      }
    ).then((res) => { return res.json(); }).then((json) => {

    }).catch((err) => {
      ElMessage.error(err.message); loading.value = false;
    });
  }
};
const handleUp = (type) => {
  let d = currentData.value
  if (d) {
    // ptz控制command：['left','right','up','down','stop'] horizonSpeed:水平速度0-255  verticalSpeed:垂直速度0-255
    fetch(`http://39.150.**.**:8/api/front-end/ptz/${d.wvpDeviceId}/${d.wvpChannelId}?command=stop&horizonSpeed=50&verticalSpeed=50&zoomSpeed=4`,
      {
        method: "GET",
        headers: {
          "access-token": `${myAccessToken}`,
          "Content-Type": "application/json",
        },
      }
    ).then((res) => { return res.json(); }).then((json) => {

    }).catch((err) => {
      ElMessage.error(err.message); loading.value = false;
    });
  }
};
```

